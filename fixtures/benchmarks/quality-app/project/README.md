# Codument Quality Fixture

Small dependency-free app for benchmarking docs-backed agent work.

Run the current tests:

```bash
npm test
```

The benchmark task is written to `BENCHMARK_TASK.md` by `codument benchmark init`.
