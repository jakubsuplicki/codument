# Quality App Fixture

This fixture powers `codument benchmark init` and the planned `codument benchmark score`.

It is a small dependency-free Node app with source files, tests, docs, a registry, and a benchmark task prompt. `benchmark init` copies `project/` into the requested target directory, installs Codument agent assets, and writes `BENCHMARK_TASK.md`.
