# Context Routing Fixture

This fixture powers `codument benchmark context`.

The task asks for a change in meal planning. The fixture includes nearby but irrelevant product areas so the benchmark can compare a naive whole-project context against Codument's registry-guided working set.
